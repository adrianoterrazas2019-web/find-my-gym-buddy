<% if workout_plans.empty? %>
  <p>No workout plans yet.</p>
<% else %>
  <ul>
    <% workout_plans.each do |plan| %>
      <li>
        <strong><%= plan.title.presence || "Workout Plan ##{plan.id}" %></strong>
        <% if plan.description.present? %>
          <p><%= plan.description %></p>
        <% end %>
      </li>
    <% end %>
  </ul>
<% end %>
